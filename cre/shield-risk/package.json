{
  "name": "shield-risk-workflow",
  "version": "1.0.0",
  "private": true,
  "description": "Shield's confidential loss-rule evaluation (Chainlink CRE, handlerInTee). Dependencies resolve from the repo root.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "compile": "bunx cre-compile main.ts dist/shield-risk.wasm"
  }
}
