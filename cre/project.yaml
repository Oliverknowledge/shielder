# ==========================================================================
# CRE PROJECT SETTINGS — Shield
# ==========================================================================
# The Shield risk workflow makes no chain reads or writes from CRE itself:
# the enclave signs an Ed25519 verdict that Shield's relayer submits to the
# Solana vault program, which verifies it against the key the USER pinned.
# RPCs are listed so the CLI's chain validation is satisfied and so the
# native `SolanaClient.writeReport` path (see README) can be enabled later.
staging-settings:
  rpcs:
    - chain-name: solana-devnet
      url: https://api.devnet.solana.com
    - chain-name: ethereum-testnet-sepolia
      url: https://ethereum-sepolia-rpc.publicnode.com

production-settings:
  rpcs:
    - chain-name: solana-mainnet
      url: https://api.mainnet-beta.solana.com
    - chain-name: ethereum-mainnet
      url: https://ethereum-rpc.publicnode.com

# EVM-only target. The Shield workflow makes no chain calls from CRE at all —
# the enclave signs an EIP-712 verdict and Shield's relayer submits it — but
# the CLI validates every RPC a target declares, so listing solana-devnet made
# an EVM simulation demand a funded Solana keypair it never uses.
evm-settings:
  rpcs:
    - chain-name: ethereum-testnet-sepolia
      url: https://ethereum-sepolia-rpc.publicnode.com

# The same EVM target with delivery off, so a judge (or a rehearsal) can run the
# workflow end to end without broadcasting a verdict to a live vault. Every other
# target has `deliver: true`, which meant there was no safe way to simulate.
evm-dry-settings:
  rpcs:
    - chain-name: ethereum-testnet-sepolia
      url: https://ethereum-sepolia-rpc.publicnode.com
